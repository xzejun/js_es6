const arr = [1, 2, 3, 4];

// arr.push(5);
// arr.unshift(5);

// arr.shift();
// arr.pop();
// arr.splice(0, 2)  //参数: start length
// arr.slice(0, 2)  //跟 splice比较： 不改变原有数组，返回值为切割部分

// arr 方法比较重要 大部分需要会使用

// console.log(arr.join('/'))
// console.log(arr.includes(1))  //ES7
//console.log(arr.indexOf(2))
// arr.sort((a, b) => b - a) // 倒叙

// console.log(arr)


// const a = arr.map(v => v + 1);

// arr.forEach(v => console.log(v))
// console.log(a);



